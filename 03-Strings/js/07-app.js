const producto = 'Monitor 20 Pulgadas ';
// Con este método lo llevamos todo a Mayúsculas
console.log(producto.toUpperCase());

// Con este método lo convertimos todo a minúsculas
console.log(producto.toLowerCase());

const precio = 300;
console.log(precio);

// Para convertir este número a String
console.log(precio.toString());